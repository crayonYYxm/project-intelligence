# 质量报告

## Hard 规范

_None detected._

`failed` 会导致检查失败；`manual-review` 必须由 Agent/评审人员核对，但不会被 CLI 误判为失败。

## 命令

_检测到 1 条质量命令，本次未使用 `--run-quality`，因此没有执行。_

## 命令输出

_本次没有命令输出。_

## 冗余

- 前端冗余候选数：0
- 后端候选入口点数：0

冗余发现默认为 `candidate`，除非团队策略升级，否则不会导致检查失败。
